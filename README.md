# Synapse

## Catalyst

- `/catalyst` -> List of all ServiceLocators
- `/catalyst/aggregate/{locator}` -> send a request to the Aggregation of all Specified ServiceLocators
- `/catalyst/request/{id}` -> send a tailored request to a specific Service
- `/catalyst/mcp` -> an MCP Server for LLM Agents